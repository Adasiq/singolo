//=====================MENU BAR=====================//

const menubar = document.getElementsByTagName('body');
const menu = document.getElementsByClassName('menu');
menu.querySelectorAll('a');

function changeOn(){
   menuLink.style.active;
}

document.addEventListener("click", changeOn());
